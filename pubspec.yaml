name: parcial_2
description: "Parcial Flutter 2 - Accidentes Tuluá + CRUD Establecimientos"
publish_to: 'none'
version: 1.0.0+1
environment:
  sdk: '>=2.19.0 <4.0.0'
dependencies:
  flutter:
    sdk: flutter
  dio: ^5.4.0
  go_router: ^17.0.0
  flutter_dotenv: ^6.0.0
  fl_chart: ^0.68.0
  skeletonizer: ^2.0.0
  image_picker: ^1.0.7
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
flutter:
  uses-material-design: true
  assets:
    - .env
